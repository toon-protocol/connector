name: CI

on:
  push:
    branches:
      - main
  # Every base branch, not just `main` (issue #1152). A `pull_request` trigger
  # filters on the PR's BASE, so `branches: [main]` meant a stacked PR — one
  # opened against another PR's branch — ran none of the jobs below. That did
  # not surface as pending or skipped; it surfaced as nothing at all, while
  # `local-topologies.yml` (which has no branch filter) went green beside it.
  # #1149 sat at four green ticks carrying an 832-line Rust integration test
  # on which `cargo fmt`, `cargo clippy -D warnings` and `cargo test
  # --workspace` had never run once; it had to be recreated as #1150 against
  # `main` to get a real signal.
  #
  # The cost is real and deliberately accepted: the Rust gate is ~8-12 minutes
  # and every stacked PR now pays it (the `concurrency` group below is what
  # keeps that bill down). The cheaper alternatives were both refused. A
  # required status check cannot reach this case as configured: branch
  # protection is a property of the BASE branch, `main` is the only protected
  # branch (it requires `CI Status Summary`), and a stacked PR's base is an
  # unprotected feature branch — so no rule applies, and even a repo-wide
  # ruleset would only block the merge with "Expected", never make the gate
  # run. And "just never stack" is a convention with nothing enforcing it,
  # which is the class of problem #1150 was merged to fix.
  #
  # `push` stays on `main`: that is the post-merge signal and wants no
  # widening. Every branch's pre-merge signal already arrives via the PR.
  pull_request:
    branches:
      - '**'

# Widening the trigger above put every stacked PR on the full Rust gate, so
# stop paying for runs that are already obsolete: a force-push or a new commit
# cancels the run it supersedes instead of racing it. PR runs group by PR
# number. `push` runs on `main` group by their own commit sha, so they can
# never cancel one another — every merged commit keeps its own post-merge
# signal, which is the one run nobody may lose.
concurrency:
  group: ci-${{ github.event.pull_request.number || github.sha }}
  cancel-in-progress: true

jobs:
  # A docs-only pull request does not pay for the chain-bound gate. Before
  # this job existed a README edit cost the full Rust workspace gate, two
  # Solana SBF builds and a devbox validation -- ten-plus minutes of runner
  # time for a change nothing under crates/ could see. This job says what
  # the PR touched, and `ci-status` at the bottom holds every heavy job to
  # that verdict: a skip counts as a pass ONLY when this job said the gate
  # had nothing to check, never because a job was cancelled or never ran.
  #
  # "Docs-only" is deliberately narrow -- every changed path is a Markdown
  # file or lives under docs/ -- and it is NOT "test-free". Markdown is read
  # by tests here: records_state_their_own_falsifier.rs walks all of docs/
  # plus README.md, CONTEXT.md and CLAUDE.md; vendored_rfcs_are_unmodified.rs
  # diffs docs/rfcs/; documented_config_keys.rs reads
  # docs/protocol/configuration-spec.md; solana_program_ids.rs walks every
  # tracked file. So a docs-only PR runs exactly those harnesses in
  # `docs-gate` below, with no chain binary installed, instead of the whole
  # workspace. Anything that is not Markdown -- a workflow, a TOML, a key
  # file, a Makefile -- is code and gets the full gate.
  #
  # A `push` to main is never docs-only: the post-merge signal stays whole.
  changes:
    name: What changed
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.classify.outputs.code }}
    steps:
      - name: Classify the changed paths
        id: classify
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          if [ "${{ github.event_name }}" != "pull_request" ]; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "not a pull request: full gate"
            exit 0
          fi
          # The files API needs no checkout. Renames are classified by both
          # names, so a file moved out of docs/ is code.
          gh api --paginate "repos/${{ github.repository }}/pulls/${PR}/files" \
            --jq '.[] | .filename, (.previous_filename // empty)' > changed.txt
          echo "changed paths:"; sed 's/^/  /' changed.txt
          # An empty list is an API failure, not an empty PR (the empty-PR
          # guard runs elsewhere); 3000 is where that API stops listing.
          if [ ! -s changed.txt ] || [ "$(wc -l < changed.txt)" -ge 3000 ]; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "could not classify every path: full gate"
            exit 0
          fi
          if grep -vE '^(docs/|.*\.md$)' changed.txt; then
            echo "code=true" >> "$GITHUB_OUTPUT"
            echo "-> the paths above are not documentation: full gate"
          else
            echo "code=false" >> "$GITHUB_OUTPUT"
            echo "-> docs only: the record-reading tests, nothing chain-bound"
          fi

  # The connector itself is Rust (ADR 0017) — `rust-gate` below is this repo's
  # real gate. The npm surface that remains is devnet tooling only (the faucet,
  # the announcer, and tools/fund-peers), so this job just keeps it lint-clean
  # and formatted.
  lint-and-format:
    name: Lint and Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Verify Node and npm versions
        run: |
          echo "Node version: $(node --version)"
          echo "npm version: $(npm --version)"
          echo "Current directory: $(pwd)"

      - name: Install dependencies
        run: |
          echo "Installing dependencies..."
          npm ci --ignore-scripts --verbose
          echo "Installation complete. Verifying..."
          npm list --depth=0 || true

      - name: Verify workspace setup
        run: |
          echo "Checking workspace configuration..."
          npm query ".workspace" | head -20 || true
          echo "Checking for binaries..."
          ls -la node_modules/.bin/ | head -20 || true

      - name: Run ESLint
        run: |
          echo "Running ESLint across all workspaces..."
          npm run lint --verbose

      - name: Check Prettier formatting
        run: |
          echo "Checking Prettier formatting..."
          npm run format:check

  # Fails when a key-shaped file (a Solana/EVM keypair, a `.key`/`.secret`, a
  # wallet file) is tracked in git — the guard connector#920/#922 asked for
  # after crates/connector-settlement-solana/deploy/payment_channel-keypair.json
  # sat tracked with no `.gitignore` rule covering it at all. An ignore rule
  # alone does not catch this class of gap: it only stops a *new* commit from
  # adding a matching file, not one already in the index, which is exactly how
  # that keypair survived. This job inspects `git ls-files`, so it catches
  # both. See tools/ci/check-tracked-secrets.sh for the allowlist and the
  # pattern list.
  tracked-secrets:
    name: Tracked Key Guard
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for tracked key-shaped files
        run: bash tools/ci/check-tracked-secrets.sh

      # `fleet-health.yml`'s peering cross-check runs on a 15-minute cron and
      # nowhere else, so a PR that breaks it would go green here and be found
      # by a monitor silently reporting health it never checked. Its FAIL
      # branches are unreachable from a healthy fleet, so `--self-test` drives
      # every one of them against synthetic self-descriptions. It reads no
      # network and no credential; it belongs on every PR.
      - name: The fleet peering cross-check's failure branches still fire
        run: python3 tools/ci/fleet-peering-crosscheck.py --self-test

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true'
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.12.0'
          cache: 'npm'

      - name: Install dependencies (including optional deps)
        # `npm ci` installs optionalDependencies by default on supported
        # platforms; we pass --include=optional explicitly so the audit below
        # is guaranteed to see optional packages regardless of any future
        # default change.
        run: npm ci --ignore-scripts --include=optional

      # Security hardening: Check for high/critical vulnerabilities (Story 16.2)
      # Currently advisory due to transitive dependency issues requiring major updates
      # Known issues: express (body-parser), aws-sdk (fast-xml-parser)
      #
      # Epic 35 retro action item #2: ensure optional deps are in-scope. We
      # run audit twice: once over the full tree (default), and once
      # explicitly including optional deps via --include=optional. The second
      # pass is defensive — `npm audit` should already cover installed
      # optional deps, but being explicit documents intent and survives any
      # npm behavior change.
      - name: Run npm audit (full tree including optional deps)
        run: npm audit --audit-level=high --include=optional
        continue-on-error: true

      - name: Run npm audit (baseline, all installed packages)
        run: npm audit --audit-level=high
        continue-on-error: true

      # Security hardening: Check for Snyk high/critical findings (Story 16.2)
      # Advisory - fails gracefully if SNYK_TOKEN not configured
      - name: Run Snyk security scan
        uses: snyk/actions/node@master
        continue-on-error: true
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          args: --severity-threshold=high

      - name: Upload Snyk results to GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: snyk.sarif

  # Gate for the Cargo workspace (crates/*, packages/solana-program) — the
  # connector itself. Runs unconditionally, so a newly added crate is covered by
  # build/test/clippy/fmt without editing this job. payment-channel
  # (packages/solana-program) is excluded from test/clippy: it needs the Solana
  # SBF toolchain to test meaningfully (covered by the solana-program job below)
  # and pre-dates this gate's warnings-denied clippy policy.
  rust-gate:
    name: Rust Workspace Gate
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true'

    steps:
      # `submodules: recursive` (matching contracts.yml): connector-settlement-evm's
      # abi_provenance test (issue #572) runs a real `forge build` of packages/contracts,
      # which needs its lib/openzeppelin-contracts and lib/forge-std submodules checked out.
      # Without this, forge build fails on unresolved imports and the gate reports a false
      # failure that has nothing to do with the Rust code it's meant to be testing.
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo registry and workspace target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: rust-gate-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            rust-gate-${{ runner.os }}-

      # anvil (and cast, used by chain-integration tests to talk to it) on PATH for any
      # crate's tests -- connector-settlement-evm's are the first, connector-settlement-solana
      # and future settlement crates will need the equivalent Solana tooling separately.
      # Without this, an anvil-gated test silently skips and reports `passed` (issue #471).
      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          # Pinned, not `nightly`. The EVM settlement tests were verified green
          # against anvil 1.7.1 (contract suite 1.01s, gas/nonce 0.60s), so the
          # gate installs that exact version rather than whatever nightly is on
          # the day. A gate that exists to prove chain code works should not be
          # able to break from an upstream build we never ran against.
          # contracts.yml is pinned to the same version (was `nightly` until
          # the 2026-08-14 1.8.0-nightly fork-test regression proved this
          # comment's point).
          version: v1.7.1
        env:
          # foundryup curls api.github.com unauthenticated and gets 403 when the runner IP is
          # rate-limited (same fix as contracts.yml). Exporting the workflow's
          # auto-provisioned GITHUB_TOKEN bumps the limit from 60 -> 5000 req/hr.
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # solana-test-validator on PATH for connector-settlement-solana's tests (issue #428), the
      # Solana twin of the Foundry install above. Without this, a solana-test-validator-gated
      # test would silently skip and report `passed` -- exactly the defect #471 closed for
      # anvil, see crates/connector-settlement-solana/tests/support/mod.rs.
      #
      # Pinned to v2.1.21, NOT the v3.1.12 the `solana-program` jobs below install. This
      # repository installs two Solana CLIs on purpose and they are not interchangeable;
      # crates/connector-settlement-solana/tests/solana_cli_pins.rs records which goes where,
      # why, and what would have to be true to change either, and fails the build if this
      # literal drifts from it. In short: v3's solana-test-validator hard-requires io_uring,
      # which the agent sandbox's seccomp profile does not permit, and v2.1.21 matches the
      # `=2.1.0` Solana crate pins the workspace compiles the program against.
      # `.sandcastle/Dockerfile` installs this exact version for the same reason Foundry is
      # pinned identically in both places: an agent that passes locally on a different
      # toolchain than the gate has learned nothing.
      - name: Install Solana CLI tools
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v2.1.21/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      # connector-settlement-solana's test harness builds packages/solana-program with
      # the pinned platform-tools line when target/deploy/payment_channel.so is not
      # already in the cached `target` dir (issue #567) -- cache the platform-tools
      # install exactly like the solana-program job below does, so a workspace-cache miss
      # doesn't also re-download the SBF toolchain inside a test run.
      #
      # A speed-up, not a dependency: the harness builds through
      # tools/solana/build-sbf.sh, which bootstraps $HOME/.cache/solana from cold.
      # A miss here costs a download, not a red job.
      - name: Cache Solana platform-tools
        uses: actions/cache@v4
        with:
          path: ~/.cache/solana
          key: solana-platform-tools-${{ runner.os }}-v1.52

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Build workspace
        run: cargo build --workspace

      - name: Run tests
        # CI=true is already set by GitHub Actions for every job; anvil-gated and
        # solana-test-validator-gated tests both key off it (rather than a bespoke
        # REQUIRE_ANVIL/REQUIRE_SOLANA_TEST_VALIDATOR nobody remembers to set) to hard-fail here
        # instead of silently skipping when the chain binary isn't on PATH -- see
        # crates/connector-settlement-evm/tests/support/mod.rs and
        # crates/connector-settlement-solana/tests/support/mod.rs.
        #
        # `shell: bash` is LOAD-BEARING, not style (issue #652). The default for a
        # `run:` block is `bash -e {0}` -- no `pipefail` -- so this pipeline's exit
        # status was `tee`'s, always 0, and cargo test's failure was discarded. The
        # gate reported SUCCESS over `test result: FAILED` for as long as the pipe
        # existed (proven on #645). `shell: bash` runs `bash --noprofile --norc -eo
        # pipefail {0}`, so the pipe keeps feeding the empty-harness check below
        # (#471) while a test failure actually fails the job. Do not drop it, and do
        # not "simplify" this back to a bare `run:` with a pipe.
        shell: bash
        run: cargo test --workspace --exclude payment-channel 2>&1 | tee /tmp/cargo-test.log

      - name: Assert no integration-test harness executed zero tests
        # A `tests/*.rs` integration binary exists specifically to hold tests. Unlike a crate's
        # `unittests`/doctests target -- which legitimately reports "running 0 tests" when the
        # crate simply has none -- a dedicated harness reporting zero is always a bug: a feature/
        # cfg mismatch left it compiling with nothing inside, and cargo still reports the target
        # `ok`. Issue #471 ("a target that executes zero tests does not report success").
        run: |
          awk '
            /^ *Running tests\// { harness = $0; next }
            /^running 0 tests$/ && harness { print "EMPTY TEST HARNESS (reports success despite running nothing): " harness; found = 1; harness = "" }
            /^running [0-9]/ { harness = "" }
            END { if (found) exit 1 }
          ' /tmp/cargo-test.log

      - name: Clippy (warnings denied)
        run: cargo clippy --workspace --exclude payment-channel --all-targets -- -D warnings

  # The docs-only half of the gate (see `changes` above): the harnesses that
  # read Markdown, and only those. No Foundry, no Solana CLI, no submodules --
  # none of these tests touch a chain, and the EVM bindings build from the
  # committed ABI. The `target` cache is restored from rust-gate's key and
  # deliberately never saved from here, so a partial build can never become
  # what the full gate restores.
  docs-gate:
    name: Docs Gate (record-reading tests)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'false'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Cargo registry and workspace target
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: rust-gate-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            rust-gate-${{ runner.os }}-

      - name: Run the tests that read Markdown
        # `shell: bash` for the same reason as rust-gate: the pipe into tee
        # must not swallow cargo's exit status (issue #652).
        shell: bash
        run: |
          cargo test -p connector \
            --test records_state_their_own_falsifier \
            --test vendored_rfcs_are_unmodified \
            --test documented_config_keys \
            --test production_skeleton_is_inert 2>&1 | tee /tmp/cargo-test.log
          cargo test -p connector-settlement-solana \
            --test solana_program_ids 2>&1 | tee -a /tmp/cargo-test.log

      - name: Assert no harness executed zero tests
        # Same check as rust-gate (issue #471): a harness reporting
        # `running 0 tests` is a broken harness, not a pass.
        run: |
          awk '
            /^ *Running tests\// { harness = $0; next }
            /^running 0 tests$/ && harness { print "EMPTY TEST HARNESS (reports success despite running nothing): " harness; found = 1; harness = "" }
            /^running [0-9]/ { harness = "" }
            END { if (found) exit 1 }
          ' /tmp/cargo-test.log

  # Solana program tests (Rust/BPF) — packages/solana-program is the Cargo
  # crate `payment-channel`, a Cargo workspace member, not a TypeScript package.
  solana-program:
    name: Solana Program Tests (Rust)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # v3.1.12, NOT the v2.1.21 the rust-gate above installs -- the two are deliberate and
      # not interchangeable. This job builds the program, and the deploy runbook
      # (docs/solana-deployment.md) requires a CLI >= 3.1.12, so `cargo test-sbf` here runs
      # the same toolchain driver the deploy path does. See
      # crates/connector-settlement-solana/tests/solana_cli_pins.rs, which records both pins
      # and fails the build if this literal drifts from them.
      - name: Install Solana CLI tools
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.12/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      # Caching the platform-tools dir keyed on the tools-version avoids the
      # flaky network resolution panic (issue #105).
      - name: Cache Solana platform-tools
        uses: actions/cache@v4
        with:
          path: ~/.cache/solana
          key: solana-platform-tools-${{ runner.os }}-v1.52

      # `cargo test-sbf` is cargo-build-sbf underneath, and that reads
      # $HOME/.cache/solana before creating it -- a cold cache is a panic, not a
      # download (tools/solana/build-sbf.sh's header). This job cannot go through
      # that script (it runs test-sbf, not build-sbf), so it pre-creates the
      # directory the same way the reproducibility job below does. A no-op once
      # the cache restores.
      - name: Ensure platform-tools cache directory exists
        run: mkdir -p ~/.cache/solana

      - name: Cache Solana program build
        uses: actions/cache@v4
        with:
          path: |
            target
          key: solana-program-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            solana-program-${{ runner.os }}-

      - name: Run Solana program tests
        working-directory: packages/solana-program
        # Retry on the flaky platform-tools version-resolution panic (issue #105):
        # the first run on a cold platform-tools cache still hits the network and
        # can panic ("unexpected character 'l' while parsing major version").
        run: |
          n=0
          until cargo test-sbf --tools-version v1.52; do
            n=$((n + 1))
            if [ "$n" -ge 3 ]; then
              echo "cargo test-sbf failed after $n attempts"
              exit 1
            fi
            echo "cargo test-sbf attempt $n failed (likely flaky tools-version resolution) — retrying in 5s..."
            sleep 5
          done

  # Build-reproducibility check for the mainnet deploy path (issue #954, the
  # no-broadcast part of #834). Builds packages/solana-program TWICE, from clean,
  # with the same pinned v1.52 platform-tools line the live devnet program was built
  # from (packages/solana-program/deployments/devnet-public.md, "Reproducible-build
  # comparison" -- that line matches the live bytes exactly in size and 99.66%
  # byte-identically; a zero-diff against the live program would additionally need the
  # deploy-time lockfile and build path, which were not preserved), and asserts the two
  # artifacts hash identically. No deploy and no key material; the only network traffic
  # is fetching the toolchain and the crate dependencies (this job restores no ~/.cargo
  # cache, so build #1 resolves the dependency tree from crates.io against the pinned
  # workspace Cargo.lock) -- this proves the SOURCE rebuilds bit-for-bit from a clean
  # SBF target, which is what the mainnet runbook's human deploy step relies on being
  # true before it ever runs.
  solana-program-reproducibility:
    name: Solana Program Build Reproducibility (mainnet deploy path)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # v3.1.12, and here the choice is load-bearing rather than merely consistent: with
      # --tools-version v1.52 fixed on both sides, the CLI driving the build still changes the
      # .so's bytes, so this job blesses the bytes of whichever CLI it installs. It has to be
      # the one docs/solana-deployment.md tells a human to deploy with (>= 3.1.12). The
      # measurement, and the two pins' full rationale, are in
      # crates/connector-settlement-solana/tests/solana_cli_pins.rs, which also fails the build
      # if this literal drifts.
      - name: Install Solana CLI tools
        run: |
          sh -c "$(curl -sSfL https://release.anza.xyz/v3.1.12/install)"
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH

      # Purely a speed-up now: both builds below go through
      # tools/solana/build-sbf.sh, which creates $HOME/.cache/solana itself (the
      # cold-cache panic this step's `mkdir -p` used to hand-patch) and retries
      # the flaky version resolution (issue #105). A miss here costs a download,
      # not a red job.
      - name: Cache Solana platform-tools
        uses: actions/cache@v4
        with:
          path: ~/.cache/solana
          key: solana-platform-tools-${{ runner.os }}-v1.52

      # Quoted: an unquoted YAML scalar treats " #1 (clean)" as a comment, which
      # collapsed these four step names to "Build"/"Hash build" in the run log and
      # hid which of the two builds a failure came from.
      - name: "Build #1 (clean)"
        working-directory: packages/solana-program
        run: ../../tools/solana/build-sbf.sh

      # No working-directory here on purpose: packages/solana-program is a member of
      # the root Cargo workspace, so cargo build-sbf writes to the workspace-root
      # target/, the same path tools/solana/deploy.sh reads. Removing target/deploy
      # and the SBF target dir makes build #2 recompile the program from source
      # instead of re-copying build #1's artifact; host-side build artifacts are left
      # alone because nothing from them lands in the .so.
      - name: "Hash build #1 and reset build output"
        run: |
          sha256sum target/deploy/payment_channel.so | awk '{print $1}' > /tmp/build1.sha256
          echo "Build #1 sha256: $(cat /tmp/build1.sha256)"
          # build-sbf.sh builds in a cargo target directory of the pinned
          # platform-tools line's own — see reason (4) in its header — so the
          # directory to reset is now named, not guessed. It used to be
          # guessed: cargo-build-sbf's own target triple is the CLI's
          # (rust_target_triple() in Agave), sbf-solana-solana on the 2.1 line
          # and sbpf-solana-solana on 3.x, so this step had to try both.
          # FAIL if it is not there, because rm'ing a nonexistent dir is
          # exactly how this gate was once a tautology: build #2 recompiled
          # nothing and the two hashes were equal by construction.
          line_dir="target/sbf-tools-$(tools/solana/build-sbf.sh --print-tools-version)"
          if [ ! -d "$line_dir" ]; then
            echo "::error::$line_dir does not exist — build #1 did not build where this step"
            echo "::error::resets, so build #2 would recompile nothing and this job would"
            echo "::error::assert reproducibility by construction. Reconcile this with"
            echo "::error::tools/solana/build-sbf.sh's --target-dir."
            exit 1
          fi
          rm -rf "$line_dir"
          echo "Reset $line_dir"
          rm -rf target/deploy

      - name: "Build #2 (clean)"
        working-directory: packages/solana-program
        run: ../../tools/solana/build-sbf.sh

      - name: "Hash build #2 and assert reproducibility"
        run: |
          sha256sum target/deploy/payment_channel.so | awk '{print $1}' > /tmp/build2.sha256
          echo "Build #2 sha256: $(cat /tmp/build2.sha256)"
          if ! diff -q /tmp/build1.sha256 /tmp/build2.sha256 > /dev/null; then
            echo "::error::Two clean builds of packages/solana-program with the pinned v1.52"
            echo "::error::toolchain produced DIFFERENT artifact hashes. The mainnet deploy"
            echo "::error::runbook (docs/solana-deployment.md) depends on this build being"
            echo "::error::reproducible -- investigate before deploying anything built from"
            echo "::error::this commit."
            exit 1
          fi
          echo "Reproducible: both clean builds hash identically ($(cat /tmp/build1.sha256))."

  # Summary job to provide clear CI status -- and the ONLY status branch
  # protection requires, which is why it runs on every PR (`if: always()`)
  # and why the docs-only skip is decided here and not by a workflow-level
  # path filter: a path-filtered required check never reports, and the PR
  # sits at "Expected" forever.
  ci-status:
    name: CI Status Summary
    runs-on: ubuntu-latest
    needs:
      [
        changes,
        lint-and-format,
        rust-gate,
        docs-gate,
        security,
        solana-program,
        solana-program-reproducibility,
        devbox-validate,
        tracked-secrets,
      ]
    if: always()

    steps:
      - name: Check CI Results
        run: |
          CODE="${{ needs.changes.outputs.code }}"
          echo "=========================================="
          echo "CI/CD Pipeline Summary"
          echo "=========================================="
          echo ""
          echo "Change classification: ${{ needs.changes.result }} (code=${CODE:-unset})"
          echo ""
          echo "Job Results:"
          echo "  Lint & Format: ${{ needs.lint-and-format.result }}"
          echo "  Tracked Key Guard: ${{ needs.tracked-secrets.result }}"
          echo "  Rust Workspace Gate: ${{ needs.rust-gate.result }}"
          echo "  Docs Gate: ${{ needs.docs-gate.result }}"
          echo "  EVM Contracts: see Contracts workflow (.github/workflows/contracts.yml)"
          echo "  Solana Program: ${{ needs.solana-program.result }}"
          echo "  Solana Build Reproducibility: ${{ needs.solana-program-reproducibility.result }}"
          echo "  Devbox Validation: ${{ needs.devbox-validate.result }}"
          echo "  Security: ${{ needs.security.result }} (advisory)"
          echo ""

          fail=0
          # The fleet rule from toon-meta#279: a skipped or cancelled gating
          # job must never read as a pass. `always` is a job that runs on
          # every PR and must succeed. `heavy` is a job the `changes` verdict
          # may skip: on a code change it must succeed; on a docs-only change
          # it must have been skipped by that verdict (or, if it ran anyway,
          # succeeded) -- a failure or a cancellation is a failure either way.
          always() {
            if [[ "$2" != "success" ]]; then echo "❌ $1: $2"; fail=1; else echo "✅ $1"; fi
          }
          heavy() {
            if [[ "$CODE" == "true" ]]; then
              always "$1" "$2"
            elif [[ "$2" == "skipped" ]]; then
              echo "⏭️  $1: skipped (docs-only change)"
            else
              always "$1" "$2"
            fi
          }

          always "What changed" "${{ needs.changes.result }}"
          if [[ "$CODE" != "true" && "$CODE" != "false" ]]; then
            echo "❌ the change classification produced no verdict"; fail=1
          fi
          always "Lint & Format" "${{ needs.lint-and-format.result }}"
          always "Tracked Key Guard" "${{ needs.tracked-secrets.result }}"
          heavy "Rust Workspace Gate" "${{ needs.rust-gate.result }}"
          heavy "Solana Program" "${{ needs.solana-program.result }}"
          heavy "Solana Build Reproducibility" "${{ needs.solana-program-reproducibility.result }}"
          heavy "Devbox Validation" "${{ needs.devbox-validate.result }}"
          if [[ "$CODE" == "false" ]]; then
            always "Docs Gate" "${{ needs.docs-gate.result }}"
          elif [[ "${{ needs.docs-gate.result }}" != "skipped" && "${{ needs.docs-gate.result }}" != "success" ]]; then
            always "Docs Gate" "${{ needs.docs-gate.result }}"
          fi
          # Security is advisory (transitive dependency vulnerabilities) and
          # does not gate.

          echo ""
          if [[ "$fail" -ne 0 ]]; then
            echo "❌ CI Pipeline FAILED"
            echo ""
            echo "Please check the failed jobs above for details."
            exit 1
          fi
          echo "✅ CI Pipeline PASSED"

  devbox-validate:
    name: Devbox Environment Validation
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.code == 'true' && (github.event_name == 'push' || github.event_name == 'pull_request')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install devbox
        uses: jetify-com/devbox-install-action@v0.15.0
        with:
          enable-cache: true

      # Cache the Solana CLI binary installed by the devbox init_hook
      # (written to ~/.local/share/solana by release.anza.xyz/v3.1.12/install).
      # Without this, every cold devbox-validate run re-downloads the CLI.
      # Note: platform-tools (~/.cache/solana) are not cached here because this
      # job never runs cargo build-sbf and therefore never downloads them.
      - name: Cache Solana CLI
        uses: actions/cache@v4
        with:
          path: ~/.local/share/solana
          key: solana-cli-${{ runner.os }}-v3.1.12

      - name: Assert tool versions
        run: |
          # Write the assertion script to a file and run it via `devbox run -- bash
          # <file>`. Passing a multi-line script as a single `bash -c "..."` argument
          # through `devbox run --` re-tokenizes the embedded newlines (it mangled
          # `--version`\n`npm` into `grep --versionn`), so feed it as a real script
          # instead. The single-quoted heredoc keeps the regex backslashes literal.
          cat > "${RUNNER_TEMP:-/tmp}/assert-versions.sh" <<'ASSERT'
          set -euo pipefail
          node --version | grep -E '^v22\.11\.'
          npm --version | grep -E '^10\.'
          cargo --version
          rustc --version
          # foundry (forge/cast/anvil): assert they run, not a version string. The
          # devbox package `foundry@1.7.1` is the nixpkgs package version, which is
          # NOT what `forge --version` prints, so a `^forge 1\.7\.` regex is wrong.
          # devbox.lock already pins the exact foundry build.
          forge --version
          cast --version
          anvil --version
          jq --version | grep -E '^jq-1\.7\.'
          solana --version | grep -E '^solana-cli 3\.1\.12'
          ASSERT
          devbox run -- bash "${RUNNER_TEMP:-/tmp}/assert-versions.sh"
          echo "✅ All devbox tools resolved at expected versions"

      - name: Smoke build (npm ci + npm run build)
        # --ignore-scripts: skip dependency lifecycle scripts (none are required
        # for the build). Matches every other `npm ci` in this workflow; the
        # build itself is an explicit script, unaffected.
        run: devbox run -- bash -c "npm ci --ignore-scripts && npm run build"
